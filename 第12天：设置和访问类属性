# 类属性就是类对象所拥有的属性，它被该类的所有实例对象所共有。
# 类属性可以使用类对象或者实例对象进行访问。

# 示例：

class Dog():
    tooth = 10      # 这里设置的变量，就相当于是这个类的属性了。

ad = Dog()      
dd = Dog()

print(Dog.tooth)  # 类对象和实例对象都可以访问类属性
print(ad.tooth)
print(dd.tooth)   
